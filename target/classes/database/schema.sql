DROP TABLE IF EXISTS USERS;

CREATE TABLE  "USERS" (
	"USERNAME" VARCHAR2(30)  PRIMARY KEY,
	"PASSWORD" VARCHAR2(100) NOT NULL
);

DROP TABLE IF EXISTS ZAPATOS;

CREATE TABLE  "ZAPATOS" (
    
	"ZAPATO" VARCHAR2(40) PRIMARY KEY,
	"STOCK" INTEGER NOT NULL
);